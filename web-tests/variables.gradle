tasks.withType(Test) {
    if (project.hasProperty('key')) {
        systemProperty 'key',"${key}"
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser',"${browser}"
    }
    if (project.hasProperty('env')) {
        systemProperty 'env',"${env}"
    }
    if (project.hasProperty('qase')) {
        systemProperty 'qase', "${qase}"
        systemProperty 'pip_url', "${pip_url}"
    }
    if (project.hasProperty('protocol')) {
        systemProperty 'protocol',"${protocol}"
    }
    if (project.hasProperty('proxy_ip')) {
        systemProperty 'proxy_ip',"${proxy_ip}"
    }
    if (project.hasProperty('proxy_port')) {
        systemProperty 'proxy_port',"${proxy_port}"
    }
    if (project.hasProperty('proxy_timeout')) {
        systemProperty 'proxy_timeout',"${proxy_timeout}"
    }
}