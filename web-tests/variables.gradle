tasks.withType(Test) {
    if (project.hasProperty('browser')) {
        systemProperty 'browser',"${browser}"
    }
    if (project.hasProperty('version')) {
        systemProperty 'version',"${version}"
    }
    if (project.hasProperty('url_stf')) {
        systemProperty 'url_stf',"${url_stf}"
    }
    if (project.hasProperty('url_shp')) {
        systemProperty 'url_shp',"${url_shp}"
    }
    if (project.hasProperty('url_stf_front')) {
        systemProperty 'url_stf_front',"${url_stf_front}"
    }
    if (project.hasProperty('stf_forward')) {
        systemProperty 'stf_forward',"${stf_forward}"
    }
    if (project.hasProperty('bff_forward')) {
        systemProperty 'bff_forward',"${bff_forward}"
    }
    if (project.hasProperty('one_session')) {
        systemProperty 'one_session',"${one_session}"
    }
    if (project.hasProperty('url_paas_shippingcalc')) {
        systemProperty 'url_paas_shippingcalc',"${url_paas_shippingcalc}"
    }
    if (project.hasProperty('admin_url')) {
        systemProperty 'admin_url',"${admin_url}"
    }
    if (project.hasProperty('url_paas_eta')) {
        systemProperty 'url_paas_eta',"${url_paas_eta}"
    }
    if (project.hasProperty('url_paas_surgelevel')) {
        systemProperty 'url_paas_surgelevel',"${url_paas_surgelevel}"
    }
}
