rootProject.name = 'Kraken'
include 'kraken', 'api', 'reforged', 'grpc', 'jdbc', 'kafka', 'k8s', 'redis'

buildCache {
    local {
        directory = new File(rootDir, 'build-cache')
        removeUnusedEntriesAfterDays = 10
    }
}