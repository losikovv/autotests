package ru.instamart.autotests.appmanager;

import org.openqa.selenium.WebDriver;

// помощник навигации на витрине ретейлера
// тут описыввем локаторы элементов на странице
public class RetailerPageNavHelper extends NavigationHelper {
    public RetailerPageNavHelper(WebDriver driver) {
        super(driver);
    }
}
