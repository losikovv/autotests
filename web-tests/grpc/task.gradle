task grpcProductHub(type: Test) {
    useTestNG() {
        suites 'src/test/resources/suite/grpc-content.xml'
        includeGroups 'grpc-product-hub'
    }
    systemProperty 'qase.Project', 'PB'
    systemProperty 'qase.Title', 'gRPC Platform Backend Regress'
}

task grpcSalutToken(type: Test) {
    useTestNG() {
        suites 'src/test/resources/suite/grpc-content.xml'
        includeGroups 'grpc-salut-token'
    }
    systemProperty 'qase.Project', 'STS'
    systemProperty 'qase.Title', 'gRPC Salut Token Regress'
}

task grpcTagManager(type: Test) {
    useTestNG() {
        suites 'src/test/resources/suite/grpc-content.xml'
        includeGroups 'grpc-tag-manager'
    }
    systemProperty 'qase.Project', 'TM'
    systemProperty 'qase.Title', 'gRPC Tag Manager Smoke'
}